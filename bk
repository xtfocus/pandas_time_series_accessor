[tool.poetry]
name = "ts-wrangler"
version = "0.1.0"
description = "A library for fast and convenient timeseries data wrangling"
authors = ["Tung Xuan <github.com/xtfocus>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
missingno = "0.5.2"
pandas = "2.1.3"
numpy = "1.26.2"

[tool.poetry.group.test.dependencies]
pytest = "^7.4.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
pythonpath = "ts_wrangler"
